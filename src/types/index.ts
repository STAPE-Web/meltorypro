export type ISignal = {
    name: string
    owner: string
    ROI7D: number
    ROI30D: number
    maxDrowdown: number,
    copiers: number

}

export type ICard = {
    title: string
    description: string
    icon: string
}